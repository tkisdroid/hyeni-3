# 혜니캘린더 사용자 확보/유료전환 기능 구현 상세 프롬프트

당신은 `/workspace/hyeni-3` 저장소에서 작업하는 코딩 에이전트입니다.

이번 작업의 목표는 혜니캘린더의 **일상 사용 빈도**, **부모 신뢰**, **아이 참여도**, **프리미엄 전환율**을 높이기 위한 기능을 구현하는 것입니다.

핵심 구현 기능은 다음입니다.

1. 부모용 `오늘의 안심 리포트`
2. 아이용 `원탭 상태 공유`
3. 부모용 `주간 가족 리포트`
4. `가정통신문/사진 기반 AI 일정 등록` UX 강화
5. `프리미엄 구독 화면` 문구/가격 정합성 보완
6. `원격청취 감사 로그` UI 골격
7. 해외 진출 준비용 `i18n 씨앗 구조`와 문서

시간이 부족하면 **1번, 2번, typecheck, build, commit, PR**을 최우선으로 완료하세요.

---

## 0. 작업 시작 전 필수 확인

작업 시작 전에 아래 파일을 반드시 읽고 현재 구조를 파악하세요.

### 지침 문서

- `/workspace/hyeni-3/AGENTS.md`
- `/workspace/hyeni-3/CLAUDE.md`

### 앱 구조/라우팅

- `src/app/App.tsx`
- `src/app/AppShell.tsx`
- `src/app/activeChild.tsx`
- `src/app/toast.tsx`

### 부모/아이 주요 화면

- `src/screens/parent/ParentHome.tsx`
- `src/screens/parent/ParentLocation.tsx`
- `src/screens/parent/ParentSettings.tsx`
- `src/screens/child/ChildHome.tsx`
- `src/screens/shared/MemoChat.tsx`

### 구독/티어/결제

- `src/transform/tierPolicy.ts`
- `src/screens/feature/Subscription.tsx`
- `src/lib/native/billing.ts`
- `src/queries/useEntitlement.ts`

### 일정/AI/위치/기기 상태

- `src/screens/feature/AiSchedule.tsx`
- `src/screens/feature/AiCredit.tsx`
- `src/queries/useAi.ts`
- `src/queries/useSchedule.ts`
- `src/queries/useLocation.ts`
- `src/lib/api/endpoints/remote.ts`

### 디자인/공통 컴포넌트

- `src/styles/tokens.css`
- `src/styles/components.css`
- `src/components/ui/TopBar.tsx`
- `src/components/ui/SectionHeader.tsx`
- `src/lib/assets.ts`

---

## 1. 절대 규칙

### 언어/말투

- 모든 응답, 주석, 커밋 메시지, PR 본문은 한국어로 작성하세요.
- 기술 용어와 코드 식별자는 원문을 유지하세요.
- 앱 문구 톤:
  - 부모/페어링/구독/설정: 존댓말
  - 아이 모드: 반말

예:

- 부모 화면: `오늘 아이의 상태를 확인해 보세요`
- 아이 화면: `도착했어`, `부모님께 알려줄게`

### 안전/데이터

- 라이브 refresh token은 절대 조작하지 마세요.
- 테스트로 만든 데이터나 바꾼 설정은 반드시 정리하거나 최종 보고에 미정리 항목을 명시하세요.
- 비밀번호는 사용자만 입력합니다.
- 프로덕션 D1 파괴적 삭제, 스토어 배포, 시크릿 변경은 하지 마세요.
- 가짜 데이터로 성공처럼 보이게 만들지 마세요.
- 서버/스키마가 부족하면 `준비 중`, `기록이 없어요`, `아직 지원되지 않아요`처럼 정직하게 표시하세요.

### 비용/위험 기능

다음 기능은 반드시 사용자 버튼 `onClick`에서만 실행하세요.

- LLM 호출
- AI 크레딧 소모
- 결제
- 주변 소리 듣기
- force_ring
- remote-listen
- SOS
- 원격 위치 새로고침
- 권한 요청

자동 실행 금지입니다.

### 백엔드

- 백엔드는 `hyeni-1` Cloudflare Worker를 그대로 재사용합니다.
- 서버 재구축을 전제로 하지 마세요.
- 앱 단에서 기존 endpoint/쿼리/캐시를 최대한 재사용하세요.
- 서버 endpoint가 없으면 UI 골격과 빈 상태만 구현하고 최종 보고에 명시하세요.

### 활성 아이/다자녀 규칙

반드시 지키세요.

- 전역 활성 아이 스위치는 부모 홈에만 있습니다.
- 다른 화면은 다음 우선순위를 따릅니다.
  1. 딥링크/route state의 child override
  2. `activeChild`
  3. 폴백 없음
- `children[0]` 무조건 폴백 금지.
- 명시적 수신자 선택 화면만 자체 선택을 허용하되 기본값은 activeChild입니다.

### 식별자 규칙

반드시 구분하세요.

- events/supplies/memo 귀속: `family_members.id`, 즉 member id
- location/device/parent_alerts/remote-listen: auth `user_id`
- `DailySupply.child_user_id`는 이름과 달리 member id로 쓰이는 경우가 있으므로 저장값 기준으로 판단하세요.

### date_key 규칙

- date_key를 직접 조립하지 마세요.
- 반드시 `src/transform/dateKey.ts`를 사용하세요.
- 이 프로젝트의 events date_key는 월이 0-indexed 비패딩일 수 있으므로 직접 `"YYYY-MM-DD"`를 만들면 안 됩니다.

### TypeScript/CSS

- strict TypeScript 기준을 지키세요.
- `import type` 사용.
- 미사용 변수 금지.
- `<button type="button">` 사용.
- 상태는 불변 업데이트.
- CSS에서 hex 직접 사용 금지.
- 색상은 `src/styles/tokens.css` CSS 변수 사용.
- 기존 공통 클래스 우선 사용:
  - `.hy-card`
  - `.hy-press`
  - `.hy-chip`
  - `.hy-topbar`
  - `.hy-content`

---

## 2. 구현 우선순위

이번 작업은 기능이 많으므로 아래 우선순위를 따르세요.

### P0 — 반드시 완료

1. `오늘의 안심 리포트` 화면 추가
2. 부모 홈에 리포트 진입 카드 추가
3. `아이 원탭 상태 공유` 기능 추가
4. `npm run typecheck`
5. `npm run build`
6. 커밋
7. PR 작성

### P1 — 가능하면 완료

8. `주간 가족 리포트` 프리미엄 잠금/미리보기 화면
9. `AI 일정 등록` 사진/가정통신문 UX 보강
10. 구독 화면 문구/가격 정합성 점검 및 보완

### P2 — 시간이 남으면 완료

11. `원격청취 감사 로그` UI 골격
12. `i18n` 씨앗 구조
13. `docs/market-expansion-plan.md` 작성

---

# 3. P0 기능 1 — 오늘의 안심 리포트

## 3.1 목적

부모가 매일 앱을 열 이유를 만듭니다.

현재 혜니캘린더는 위치, 일정, 준비물, 메모, 기기 상태, 알림 기능이 흩어져 있습니다.  
이를 부모가 한 화면에서 빠르게 확인할 수 있도록 `오늘의 안심 리포트`를 구현합니다.

## 3.2 새 파일 후보

다음 파일을 추가하세요.

- `src/screens/feature/DailySafetyReport.tsx`
- `src/screens/feature/DailySafetyReport.css`

필요하면 보조 transform/hook 파일도 추가할 수 있습니다.

예:

- `src/transform/dailyReportView.ts`
- `src/queries/useDailySafetyReport.ts`

단, 과한 추상화는 피하고 기존 쿼리를 조합하는 정도로 시작하세요.

## 3.3 라우트

기존 라우터 구조를 확인한 뒤 PushShell 상세 화면 패턴에 맞게 라우트를 추가하세요.

추천 경로:

- `/daily-report`

또는 구조상 더 자연스러우면:

- `/parent/daily-report`

뒤로가기는 `navigate(-1)`를 사용하세요.

## 3.4 부모 홈 진입점

`src/screens/parent/ParentHome.tsx`에 카드/CTA를 추가하세요.

카드 위치는 홈 주요 안전/일정 요약 아래가 적절합니다.  
기존 카드 디자인을 해치지 않게 배치하세요.

문구 예시:

- 제목: `오늘의 안심 리포트`
- 설명: `도착, 일정, 기기 상태를 한눈에 확인해요`
- CTA: `리포트 보기`
- 보조 문구:
  - `오늘 아이의 흐름을 정리했어요`
  - `기록이 쌓이면 더 자세히 보여드려요`

## 3.5 데이터 범위

기존 쿼리/endpoint를 재사용하세요.

가능한 데이터:

1. 활성 아이 정보
2. 오늘 일정
3. 오늘 준비물
4. 최신 위치
5. 기기 상태
6. 최신 메모
7. 오늘 안전 알림/SOS/미도착 알림이 있으면 요약
8. 구독 티어

중요:

- 활성 아이가 없으면 빈 상태를 보여주세요.
- `children[0]`로 자동 폴백하지 마세요.
- 위치/기기 상태는 user_id 기준인지 확인하세요.
- 일정/준비물/메모는 member id 기준인지 확인하세요.

## 3.6 화면 구성

부모 화면이므로 존댓말을 사용하세요.

### 상단

- `TopBar` 또는 기존 상세 화면 헤더 패턴 사용
- 제목: `오늘의 안심 리포트`
- 부제: `아이의 하루를 한눈에 확인해요`

### 섹션 1 — 오늘의 상태

상태를 3단계 정도로 정리하세요.

- 안전:
  - `안전하게 확인 중이에요`
- 주의:
  - `확인이 필요한 항목이 있어요`
- 위험:
  - `긴급 알림이 있었어요`

위험 판단 기준은 기존 데이터로 가능한 범위만 사용하세요.

예:

- 오늘 SOS/긴급 알림 있음 → 위험
- 미도착/위험구역 알림 있음 → 주의 또는 위험
- 기기 상태 오래됨/위치 오래됨 → 주의
- 데이터 없음 → `아직 판단할 기록이 부족해요`

### 섹션 2 — 이동 요약

표시 후보:

- 최신 위치 주소
- 마지막 업데이트 시각
- 저장 장소 근처 여부
- 오늘 이동 기록이 있으면 간단 요약

데이터 없음 문구:

- `아직 오늘 위치 기록이 없어요`
- `위치 기록이 쌓이면 이동 흐름을 보여드려요`

무료 사용자의 위치 제한이 있다면 티어 정책을 존중하세요.  
무료에서 위치를 보여주면 안 되는 정책이면 잠금 카드로 보여주세요.

### 섹션 3 — 일정 체크

표시 후보:

- 오늘 일정 개수
- 다음 일정
- 지난 일정
- 장소가 있는 일정
- 미도착 위험이 있는 일정

데이터 없음 문구:

- `오늘 등록된 일정이 없어요`

CTA:

- `일정 추가하기`
- 기존 EventForm 경로가 있으면 연결

### 섹션 4 — 준비물

표시 후보:

- 체크 완료 수
- 남은 준비물
- 준비물 목록 3개까지

데이터 없음 문구:

- `오늘 챙길 준비물이 없어요`

### 섹션 5 — 기기 상태

표시 후보:

- 배터리
- 충전 여부
- 네트워크
- 마지막 확인 시각

데이터 없음 문구:

- `기기 상태를 확인하려면 부모 홈에서 새로고침해 주세요`

가능하면 `request_device_status`는 이 화면 진입 시 자동 호출하지 마세요.  
기기 상태 요청은 원격 명령 성격이 있으므로 버튼으로만 실행하세요.

버튼 문구:

- `기기 상태 새로고침`

버튼 클릭 시 기존 `requestDeviceStatus`/remote endpoint를 재사용하세요.

### 섹션 6 — 최신 소식

메모 최신 메시지 1~3개를 보여주세요.

데이터 없음 문구:

- `오늘 주고받은 메시지가 없어요`

CTA:

- `대화 열기`

## 3.7 프리미엄 유도 카드

무료 사용자에게는 리포트 하단에 프리미엄 유도 카드를 넣으세요.

문구:

- 제목: `프리미엄으로 더 자세히 확인하세요`
- 설명: `실시간 위치, 주간 리포트, AI 하루 요약까지 함께 볼 수 있어요`
- CTA: `프리미엄 보기`

연결:

- 기존 `/subscription` 화면 또는 구독 화면 경로

주의:

- SOS/긴급 알림은 무료로 유지한다는 정책을 해치지 마세요.

## 3.8 스타일

- 새 CSS 파일을 만들 경우 기존 디자인 토큰 사용.
- hex 직접 사용 금지.
- 모바일 우선.
- 카드형 레이아웃.
- 부모 홈과 톤을 맞추세요.

---

# 4. P0 기능 2 — 아이 원탭 상태 공유

## 4.1 목적

아이가 부모에게 부담 없이 상태를 보낼 수 있게 합니다.

“전화하기는 부담스럽고, 채팅 입력도 귀찮은” 상황에서 원탭으로 부모에게 알려주는 기능입니다.  
서버 변경 없이 기존 부모-아이 메모 스레드에 정해진 메시지를 보내는 방식으로 구현하세요.

## 4.2 수정 파일 후보

- `src/screens/child/ChildHome.tsx`
- 필요하면:
  - `src/screens/child/ChildHome.css`
  - 또는 ChildHome에서 사용하는 기존 CSS 파일

메모 전송 로직은 기존 `MemoChat` 또는 memo endpoint/hook을 재사용하세요.

확인할 파일:

- `src/screens/shared/MemoChat.tsx`
- `src/queries/useMemoThread`가 있는 파일
- `src/lib/api/endpoints/memo.ts` 또는 memo endpoint 파일

## 4.3 UI 위치

아이 홈에 카드 또는 칩 그룹을 추가하세요.

제목 예시:

- `부모님께 바로 알려줘`
- `지금 상태 보내기`

아이 모드이므로 반말:

- `버튼만 누르면 부모님께 알려줄게`

## 4.4 버튼 목록

다음 6개를 기본 제공하세요.

1. `도착했어`
2. `출발했어`
3. `늦을 것 같아`
4. `데리러 와줘`
5. `전화해줘`
6. `배터리 없어`

가능하면 아이가 누르기 쉽게 2열 또는 가로 스크롤 칩으로 구성하세요.

## 4.5 전송 메시지

각 버튼 클릭 시 다음 메시지를 전송하세요.

- `도착했어` → `나 도착했어!`
- `출발했어` → `나 출발했어!`
- `늦을 것 같아` → `조금 늦을 것 같아`
- `데리러 와줘` → `데리러 와줄 수 있어?`
- `전화해줘` → `전화해줘`
- `배터리 없어` → `배터리가 얼마 없어`

성공 토스트:

- `부모님께 보냈어`

실패 토스트:

- `보내지 못했어. 잠시 후 다시 해줘`

## 4.6 위치 공유 옵션

시간이 있으면 `내 위치도 보내기` 버튼을 추가하세요.

구현 원칙:

- 기존 MemoChat 위치 공유 로직을 재사용하세요.
- 위치 권한 요청은 버튼 클릭 시에만 하세요.
- 위치 취득 실패 시 일반 메시지로 강등하거나 실패 안내를 보여주세요.
- 위치 메시지 포맷은 기존 리치 메시지 마커를 따라야 합니다.
  - 예: `[[loc:lat,lng|주소]]`

문구:

- `내 위치 보내기`
- 성공: `위치를 보냈어`
- 실패: `위치를 찾지 못했어`

## 4.7 주의

- SOS와 혼동되면 안 됩니다.
- `데리러 와줘`, `전화해줘`는 일반 메시지입니다.
- 긴급 상황은 기존 `ChildSos`로 보내야 합니다.
- 자동 위치 전송 금지.
- childId는 member id를 사용하세요.

---

# 5. P1 기능 3 — 주간 가족 리포트

## 5.1 목적

프리미엄 전환 포인트를 만듭니다.

오늘의 안심 리포트는 무료에서도 일부 제공하고, 주간 가족 리포트는 프리미엄 전용 또는 프리미엄 중심 기능으로 둡니다.

## 5.2 새 파일 후보

- `src/screens/feature/WeeklyFamilyReport.tsx`
- `src/screens/feature/WeeklyFamilyReport.css`

필요하면:

- `src/transform/weeklyReportView.ts`

## 5.3 라우트

추천 경로:

- `/weekly-report`

기존 PushShell 패턴에 연결하세요.

## 5.4 티어 정책

`src/transform/tierPolicy.ts`를 확인하세요.

새 기능 key가 필요하면 다음과 같이 추가할 수 있습니다.

```ts
WEEKLY_REPORT: "weekly_report"
프리미엄 전용 set에 포함하세요.
단, 기존 정책과 충돌하지 않게 하세요.
5.5 무료 사용자 화면
무료 사용자는 lock/preview 화면을 보여주세요.
문구:
제목: 주간 가족 리포트
설명: 이번 주 등하교, 일정, 위치 흐름을 한 번에 정리해 드려요
잠금 문구: 주간 리포트는 프리미엄에서 사용할 수 있어요
CTA: 프리미엄 보기
프리뷰 카드 예시:
이번 주 일정 흐름
자주 머문 장소
안전 알림 요약
AI 요약
실제 데이터가 없으면 흐릿한 preview UI 또는 설명 카드만 사용하세요.
가짜 수치를 넣지 마세요.
5.6 프리미엄 사용자 화면
기존 데이터만으로 가능한 범위를 구현하세요.
표시 후보:
이번 주 일정 수
이번 주 준비물 체크 수
이번 주 메모 대화 수
이번 주 안전 알림 수
위치 이력 기반 주요 머문 곳
가장 바빴던 날
다음 주 준비할 일정
데이터 부족 문구:
아직 분석할 기록이 부족해요
일정과 위치 기록이 쌓이면 주간 흐름을 보여드려요
5.7 오늘의 안심 리포트와 연결
DailySafetyReport 하단에서 주간 리포트로 이동하는 CTA를 제공하세요.
문구:
이번 주 흐름 보기
무료이면 프리미엄으로 주간 리포트 보기
6. P1 기능 4 — 가정통신문/사진 기반 AI 일정 등록 UX 강화
6.1 목적
한국 초등 부모에게 강한 차별점인 “가정통신문 찍어서 일정 등록” 경험을 강화합니다.
6.2 확인 파일
src/screens/feature/AiSchedule.tsx
src/queries/useAi.ts
src/lib/api/endpoints/ai.ts 또는 관련 AI endpoint
src/screens/shared/MemoChat.tsx
사진/R2 업로드 관련 endpoint
6.3 UX 목표
AI 일정 등록 화면에서 텍스트/사진/음성을 명확하게 구분합니다.
탭:
텍스트
사진
음성
사진 탭의 목적은 가정통신문/알림장/학원 안내문에서 날짜·시간·장소·준비물을 추출하는 것입니다.
6.4 사진 탭 UI
문구:
제목: 가정통신문 사진으로 일정 찾기
설명: 가정통신문이나 알림장 사진을 올리면 날짜와 준비물을 찾아드려요
버튼: 사진 선택
미리보기 영역
실행 버튼: 일정 찾기
크레딧 안내:
AI가 사진에서 일정을 찾습니다. 크레딧이 사용될 수 있어요.
실패 문구:
사진에서 일정을 찾지 못했어요. 날짜가 보이게 다시 찍어 주세요.
AI 일정 등록을 사용할 수 없어요. 잠시 후 다시 시도해 주세요.
6.5 중요 실행 규칙
사진 선택만으로 AI 호출하지 마세요.
반드시 사용자가 일정 찾기 버튼을 눌렀을 때만 AI 호출하세요.
크레딧 소모 가능성을 미리 안내하세요.
AI 호출 실패 시 가짜 결과를 만들지 마세요.
텍스트만 지원하는 endpoint라면 OCR/이미지 분석을 억지로 구현하지 마세요.이 경우 사진 탭은 “준비 중” 또는 “텍스트로 옮겨 입력해 주세요”로 정직하게 강등하세요.

6.6 결과 처리
AI가 일정 후보를 반환하면 기존 일정 저장 플로우와 연결하세요.
후보 필드:
제목
날짜
시작 시간
종료 시간
장소
준비물
메모
배정 아이
배정 아이 기본값:
activeChild
저장 시:
기존 EventForm 또는 schedule mutation 재사용
date_key 직접 조립 금지
7. P1 기능 5 — 프리미엄 구독 화면 문구/가격 정합성
7.1 목적
프리미엄 구독 화면의 가격/문구를 더 명확히 하고, 코드와 UI 정합성을 점검합니다.
7.2 확인 파일
src/lib/native/billing.ts
src/screens/feature/Subscription.tsx
src/transform/tierPolicy.ts
7.3 현재 확인해야 할 점
현재 코드상 다음 상태일 수 있습니다.
월 base plan: monthly-2900
연 base plan: annual-27840
화면 표시: 월 2,900원, 연 29,000원
Google Play Console 실제 가격은 저장소만으로 확정할 수 없을 수 있습니다.
7.4 처리 원칙
확실하면 정합성을 맞추세요.
옵션 A — 코드 기준으로 UI 조정
월 2,900원
연 27,840원
보조 문구: 월 2,320원 꼴
옵션 B — 화면 기준 유지
월 2,900원
연 29,000원
annual-27840은 base plan ID일 뿐 실제 가격과 다를 수 있으므로 주석 정리
확신이 없으면 가격 숫자를 바꾸지 말고 최종 보고에 명시하세요.
7.5 문구 개선
무료/프리미엄 가치 차이를 더 명확히 하세요.
무료 문구 예시:
일정과 SOS는 무료로 시작하세요
가족 연결과 기본 안전 알림은 무료로 사용할 수 있어요
프리미엄 문구 예시:
실시간 위치, 다자녀, AI 요약까지 더 안심하세요
아이의 이동과 하루 흐름을 더 촘촘하게 확인하세요
7.6 비교표 주의
기존 tierPolicy.ts 정책과 비교표가 반드시 일치해야 합니다.
예:
무료 아이 등록 1명
리뷰 아이 등록 1명
프리미엄 아이 등록 2명
SOS/안전 알림은 항상 무료
실시간 위치는 프리미엄
8. P2 기능 6 — 원격청취 감사 로그 UI 골격
8.1 목적
주변 소리 듣기는 민감한 기능이므로, 신뢰 확보를 위해 감사 로그 화면 골격을 만듭니다.
8.2 새 파일 후보
src/screens/feature/RemoteAudioAudit.tsx
src/screens/feature/RemoteAudioAudit.css
8.3 라우트
추천 경로:
/remote-audio-audit
기존 PushShell에 연결하세요.
8.4 진입점
가능하면 다음 중 최소 1곳에 연결하세요.
원격청취 화면
부모 설정 화면
프리미엄 설명 화면
문구:
주변 소리 듣기 기록
청취 기록 보기
8.5 데이터 처리
서버에 감사 로그 endpoint가 있으면 사용하세요.
없으면 가짜 데이터를 만들지 말고 빈 상태를 표시하세요.
빈 상태 문구:
아직 표시할 청취 기록이 없어요
청취 기록은 안전 확인을 위해 저장될 예정이에요
8.6 타입 설계
향후 서버 연결을 고려해 타입을 준비할 수 있습니다.
interface RemoteAudioAuditItem {
  id: string;
  childName: string;
  startedAt: string;
  endedAt?: string;
  durationSec?: number;
  status: "started" | "ended" | "failed";
  reason?: string;
}
8.7 UI 구성
섹션:
안내 카드가족의 안전과 투명성을 위해 기록을 남깁니다

기록 목록
빈 상태
개인정보/안전 안내
주의:
실제 청취를 시작하지 마세요.
remote-listen 명령을 자동 실행하지 마세요.
이 화면은 조회/안내 전용입니다.
9. P2 기능 7 — 해외 진출 준비용 i18n 씨앗 구조
9.1 목적
일본/대만/영어권 확장을 고려해 최소한의 i18n 구조를 만듭니다.
단, 전체 앱을 대규모로 번역하지 마세요.
9.2 새 파일 후보
src/i18n/messages.ts
src/i18n/useMessage.ts
9.3 최소 구조 예시
export const messages = {
  ko: {
    dailyReportTitle: "오늘의 안심 리포트",
    dailyReportSubtitle: "아이의 하루를 한눈에 확인해요",
  },
  ja: {
    dailyReportTitle: "今日の安心レポート",
    dailyReportSubtitle: "お子さまの一日をひと目で確認できます",
  },
  "zh-TW": {
    dailyReportTitle: "今日安心報告",
    dailyReportSubtitle: "一眼確認孩子今天的狀態",
  },
  en: {
    dailyReportTitle: "Today’s Safety Report",
    dailyReportSubtitle: "See your child’s day at a glance",
  },
} as const;
간단한 hook 예시:
export function useMessage() {
  const locale = "ko";
  return messages[locale];
}
현재는 locale 변경 UI까지 만들 필요 없습니다.
새로 만든 화면 일부 문구만 이 구조를 사용할 수 있게 하세요.
9.4 주의
기존 전체 문구를 한 번에 옮기지 마세요.
대규모 리팩터 금지.
이번 작업에서는 새 화면 중심으로만 적용하세요.
10. P2 문서 — 해외 진출 계획
10.1 새 문서 후보
docs/market-expansion-plan.md
10.2 포함할 내용
다음 내용을 간단히 정리하세요.
국가 우선순위
일본
대만
동남아 일부 국가
미국은 장기 검토
일본
초등학생 등하교 안전 니즈
학원/방과후 루틴 유사
캐릭터형 UX 수용성
LINE 공유 필요
Kakao 지도 대신 Google Maps/Mapbox 필요
대만
Android 비중과 가족 안전 니즈
번체 중국어 필요
LINE/Google Maps 필요
동남아
가격 민감도가 높으므로 현지 가격 필요
$0.99~$1.99 또는 저가 연간 플랜 검토
미국
Life360, Bark, Qustodio 등 강자 존재
바로 진출보다 차별화된 niche 필요
COPPA 검토 필요
공통 준비
지도 provider 추상화
i18n
국가별 개인정보/아동 위치 정책 검토
앱스토어/플레이스토어 심사 문구 정비
11. 디자인 가이드
11.1 기존 패턴 재사용
다음 패턴을 우선 사용하세요.
TopBar
SectionHeader
.hy-card
.hy-press
.hy-chip
.hy-content
lucide-react 아이콘
asset("경로")
11.2 색상
CSS에서 hex 직접 사용 금지입니다.
사용 가능한 토큰을 src/styles/tokens.css에서 확인하고 사용하세요.
신호색 의미:
민트: 안전
앰버: 주의
레드: 위험/SOS
파랑: 정보
11.3 화면 톤
부모 화면:
포근함
신뢰
정리된 카드
과한 위협감 금지
아이 화면:
짧고 쉬운 문구
큰 버튼
반말
스티커/아이콘 활용 가능
12. 구현 중 확인할 기존 기능/훅
아래 항목은 이름이 실제와 다를 수 있으니 rg로 확인하세요.
rg -n "useMemo|Memo|memo" src
rg -n "useSchedule|Schedule|schedule" src
rg -n "useLocation|location" src
rg -n "device|health|requestDeviceStatus" src
rg -n "Subscription|tier|premium|entitlement" src
rg -n "AiSchedule|voice-parse|ai" src
환경 지침상 grep -R와 ls -R는 사용하지 마세요.
대신 rg를 사용하세요.
13. 테스트/검증
13.1 필수
수정 후 반드시 실행하세요.
npm run typecheck
npm run build
두 명령 모두 exit 0이어야 합니다.
13.2 가능하면 실행
npm run dev
브라우저 또는 실기기에서 다음 화면이 깨지지 않는지 확인하세요.
부모 홈
오늘의 안심 리포트
아이 홈
메모 화면
구독 화면
AI 일정 등록
주간 가족 리포트
원격청취 감사 로그
13.3 UI 변경 스크린샷
퍼셉터블한 웹앱 UI 변경이므로 가능하면 스크린샷을 찍어 최종 보고에 첨부하거나 경로를 남기세요.
14. 완료 기준
이번 작업은 아래 조건을 만족해야 완료입니다.
P0 기능이 구현됨
앱 빌드가 깨지지 않음
typecheck 통과
build 통과
활성 아이 규칙 위반 없음
아이/부모 문구 톤 준수
가짜 데이터 없음
자동 LLM/결제/원격제어 실행 없음
변경 사항 커밋 완료
PR 작성 완료
15. 커밋
변경 사항을 커밋하세요.
예:
git status --short
git add .
git commit -m "feat: 가족 안심 리포트와 상태 공유 기능 추가"
커밋 메시지는 conventional commits 형식의 한국어로 작성하세요.
다른 예:
git commit -m "feat: 오늘의 안심 리포트 화면 추가"
git commit -m "feat: 아이 원탭 상태 공유 추가"
git commit -m "docs: 해외 진출 준비 계획 추가"
작업 범위가 크면 논리적으로 나눠 커밋해도 됩니다.
16. PR 작성
커밋 후 PR을 작성하세요.
PR 제목 예시:
feat: 가족 안심 리포트와 상태 공유 기능 추가
PR 본문 예시:
## 요약

- 부모 홈에서 진입할 수 있는 오늘의 안심 리포트 화면을 추가했습니다.
- 아이 홈에 원탭 상태 공유 버튼을 추가했습니다.
- 주간 가족 리포트 프리미엄 진입 화면을 추가했습니다.
- AI 일정 등록의 가정통신문/사진 UX를 보강했습니다.
- 프리미엄 구독 문구와 가격 정합성을 점검했습니다.
- 원격청취 감사 로그 화면 골격을 추가했습니다.
- 해외 진출 준비 문서를 추가했습니다.

## 검증

- npm run typecheck
- npm run build

## 미해결/후속

- 서버에 주간 리포트 전용 endpoint가 없으면 기존 데이터 기반 요약만 제공합니다.
- 원격청취 감사 로그 endpoint가 없으면 빈 상태 UI만 표시합니다.
- Google Play Console 실제 연간 가격은 별도 확인이 필요할 수 있습니다.
17. 최종 보고 형식
최종 보고는 한국어로 작성하세요.
형식:
## Summary

- 변경 내용 1. 【F:파일경로†Lx-Ly】
- 변경 내용 2. 【F:파일경로†Lx-Ly】

## Testing

- ✅ `npm run typecheck`
- ✅ `npm run build`
- ⚠️ `npm run dev` — 환경상 장시간 실행은 하지 않고 빌드 검증으로 대체
각 테스트/체크 명령 앞에는 반드시 이모지를 붙이세요.
성공: ✅
환경 제약: ⚠️
실패: ❌
파일 citation은 실제 변경 파일의 줄 번호를 사용하세요.
18. 미해결/보고해야 할 가능성이 큰 항목
아래 상황이 확인되면 억지 구현하지 말고 최종 보고에 명시하세요.
서버에 주간 리포트 전용 endpoint가 없음
원격청취 감사 로그 endpoint가 없음
Google Play Console 실제 연간 가격 확인 불가
AI 사진 파싱 endpoint가 텍스트만 지원함
위치 이력 조회가 프리미엄 게이트로 막힘
activeChild가 없을 때 리포트 생성 불가
기기 상태가 on-demand라 자동 갱신할 수 없음
일부 데이터가 user_id/member_id 축이 달라 조인 불가
i18n 전체 적용은 대규모 리팩터라 이번 작업 범위에서 제외됨
가짜 데이터, 임의 성공 토스트, 임의 프리미엄 판정은 금지합니다.
19. 구현 팁
19.1 DailySafetyReport 구현 팁
처음부터 완벽한 “분석 엔진”을 만들지 말고, 기존 쿼리를 조합한 view model로 시작하세요.
예상 구조:
type DailyReportStatus = "safe" | "attention" | "danger" | "empty";

interface DailyReportView {
  status: DailyReportStatus;
  statusTitle: string;
  statusDescription: string;
  nextEventLabel?: string;
  latestLocationLabel?: string;
  deviceHealthLabel?: string;
  memoPreview?: string[];
}
19.2 아이 원탭 상태 공유 구현 팁
버튼 정의를 배열로 두면 UI와 전송 메시지를 깔끔하게 유지할 수 있습니다.
const QUICK_STATUS_ACTIONS = [
  { label: "도착했어", message: "나 도착했어!", icon: "📍" },
  { label: "출발했어", message: "나 출발했어!", icon: "🚶" },
  { label: "늦을 것 같아", message: "조금 늦을 것 같아", icon: "⏰" },
  { label: "데리러 와줘", message: "데리러 와줄 수 있어?", icon: "🚗" },
  { label: "전화해줘", message: "전화해줘", icon: "📞" },
  { label: "배터리 없어", message: "배터리가 얼마 없어", icon: "🔋" },
] as const;
단, 실제 코드에서는 emoji 사용이 기존 디자인과 맞는지 확인하고, 필요하면 lucide 아이콘 또는 asset을 사용하세요.
19.3 프리미엄 유도 팁
무료 사용자를 불안하게 만들지 말고, “더 편해짐/더 자세함”으로 설득하세요.
좋은 문구:
더 자세히 확인하고 싶을 때 프리미엄을 시작하세요
실시간 위치와 주간 리포트로 아이의 하루를 더 촘촘히 볼 수 있어요
나쁜 문구:
무료라서 볼 수 없습니다
결제해야 안전합니다
SOS/긴급 안전은 무료라는 신뢰를 유지하세요.
20. 최종 목표
이번 작업의 최종 목표는 기능을 많이 넣는 것이 아니라, 사용자가 다음 행동을 자연스럽게 하도록 만드는 것입니다.
부모:
매일 앱을 연다.
오늘 상태를 확인한다.
더 자세한 주간 흐름을 보고 싶어 프리미엄을 고려한다.
아이:
직접 타이핑하지 않아도 부모에게 상태를 보낸다.
앱을 감시 도구가 아니라 소통 도구로 느낀다.
제품:
무료 사용자는 안심하고 시작한다.
프리미엄은 실시간 위치/주간 리포트/AI 요약/다자녀로 명확히 전환된다