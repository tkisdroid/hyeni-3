# 혜니캘린더 실기기 최종 E2E 검증 & 프로덕션화 프롬프트 (Claude Fable 5 / Claude Code용)

> **사용법:** Claude Code에서 `claude-fable-5`로 세션을 열고 혜니캘린더 레포를 작업 디렉터리로 지정한 뒤 아래 전체를 프롬프트로 입력한다.
>
> **사전 준비 (세션 시작 전 TK가 확인):**
> - 최신 빌드가 **Galaxy A17(부모/교사)**, **Motorola razr 40 ultra(아이)** 에 설치되어 있고 `adb devices`에 둘 다 잡히는가.
> - iPhone은 **iOS Safari로 프로덕션 웹버전** 접속(부모 트랙).
> - Cloudflare Workers/D1/R2/Durable Objects 로그·대시보드 접근 가능한가(백엔드 검증용).
> - 구독은 **샌드박스/테스트 환경**(Qonversion)으로 결제 검증할 수 있는가.
> - 프로덕션 D1/실사용자 데이터에 파괴적 작업을 하지 않도록 테스트 계정·테스트 가족(family)을 준비한다.

---

## 역할과 미션

너는 혜니캘린더(가족 안전·일정 앱: RN/Capacitor 안드로이드 + 웹, 백엔드 Cloudflare Workers·D1·R2·Durable Objects)의 **출시 직전 최종 E2E 검증 및 프로덕션화**를 책임지는 릴리즈 엔지니어 겸 QA 리드다.

**목표:** 실제 연결된 기기에서 앱의 모든 기능을 검증하고, 발견된 문제를 **수정 → 재검증**하여 프로덕션 레벨로 끌어올린다. **내일 출시**한다.

**"완벽"의 정의(=Go/No-Go 게이트):** "무한히 검증"이 아니라 아래 릴리즈 게이트를 충족하면 완료로 판정한다.
- 미해결 **P0(출시 차단) 0건, P1(심각) 0건**
- 핵심 크로스기기 플로우(가입·로그인·**로그아웃 후 재로그인 지속**·페어링·구독 게이팅·부모↔아이 실시간·위치·SOS·**주변소리 듣기**)가 A17·razr·iPhone에서 모두 통과
- 구독 서비스 컷이 **서버에서 강제**됨(클라이언트 숨김만으로는 실패)
- 크래시 없이 소크 테스트 통과, 어떤 기기에서도 디자인 깨짐 없음
- 가족 간 데이터 격리·미성년자 개인정보/권한 처리 이상 없음
- P2/P3는 목록화하여 출시 후 fast-follow로 분리

---

## 실행 방식 — ADB 전권 자동 실행 (사람 개입 없음)

**두 실기기(A17=부모모드, razr=아이모드)를 ADB로 네가 직접 전부 몰아서 실행·검증·수정한다.** 사람의 수동 조작에 의존하지 않는다. 모든 조작을 자동화로 수행한다:

- **2기기 동시 제어:** 두 디바이스를 각각 ADB로 붙여 부모(A17)·아이(razr)를 동시에 구동한다. 한 기기에서 조작 → 다른 기기와 백엔드에서 동시에 단언한다.
- **UI 자동화:** 이 레포에 맞는 도구(Maestro/Appium/`adb shell input` 등)로 탭·입력·스와이프·스크린샷 캡처를 수행한다.
- **위치/GPS:** `adb`로 **목(mock) 위치를 주입**해 이동·지오펜스 진입/이탈을 재현한다(개발자 옵션의 mock location + appops 설정 포함). 실제 도보 이동에 의존하지 않는다.
- **SOS·포스링·오디오 모니터링:** 아이 기기에서 실제 트리거를 자동으로 발화시키고, 부모 기기 수신과 백엔드 전달 경로를 단언한다. **잠금화면·열악한 네트워크**도 ADB로 화면잠금·네트워크 조건을 조성해 재현한다.
- **소셜 로그인 OAuth:** 동의 화면까지 UI 자동화로 통과시킨다(테스트 계정 사용).
- **구독 결제:** Qonversion **샌드박스**에서 구매·복원 플로우를 자동으로 태운다.
- **푸시(FCM):** 잠금·백그라운드 상태에서 수신·탭·딥링크 콜드스타트를 자동 재현한다.
- **백엔드 단언:** Cloudflare D1/Durable Objects/로그를 조회해 실시간성·정확성·게이팅을 서버 레벨에서 확인한다.

시각적 품질·문구 등 주관 판단은 **스크린샷을 캡처해 비전으로 직접 판정**한다. 어떤 단계도 "했다고 가정"하지 말고 실제로 실행한 근거(스크린샷·로그·타임스탬프)를 남긴다.

---

## 작업 원칙 (Fable 5 최적화)

- **탐색 우선:** 코드/설정/스키마를 먼저 읽고 파이프라인·권한·엔티티 모델을 이해한 뒤 검증한다.
- **트리아지 먼저:** Phase 1 스모크로 전 기능을 빠르게 훑어 **P0 블로커 목록을 먼저 뽑고, 그것부터 수정**한다. P3 폴리시에 시간을 태우지 않는다.
- **Phase 게이트:** 각 Phase 통과 기준 충족 전 다음으로 넘어가지 않고, 종료 시 요약 보고 후 계속.
- **자동화 하네스 자가 구축:** 필요한 테스트 스크립트를 스스로 작성해 실행한다.
- **2기기 동기 검증:** 부모↔아이 플로우는 A17에서 조작 → razr에서 단언(또는 반대), 그리고 **Durable Object/D1 레이어에서도 동시에 단언**한다.
- **수정 루프:** 발견 → 원인분석 → 최소 수정 → 영향 범위 회귀 재검증.
- **증거:** 모든 pass/fail에 스크린샷·로그·타임스탬프 근거를 남긴다.
- **환각 금지:** 실제 존재하는 화면·엔드포인트·엔티티만 인용한다.

## 심각도 정의
- **P0 출시 차단:** 가입/로그인/페어링/결제·게이팅/실시간 안전기능(SOS·위치) 실패, 크래시, 데이터 유출·가족 간 데이터 혼선, 보안 결함.
- **P1 심각:** 핵심 기능 부분 실패, 잘못된 데이터, 특정 기기 UI 파손, 문구 오류로 인한 오작동 유발.
- **P2:** 사용성 저하, 경미한 시각 결함.
- **P3:** 폴리시·개선 제안.

## 기기·역할 맵
- **Galaxy A17** → 부모 모드 (부모/교사)
- **Motorola razr 40 ultra(폴더블)** → 아이 모드
- **iPhone (iOS Safari)** → 부모 웹 트랙

---

## ★ 사전 지정 필수 수정 항목 (검증과 별개로 반드시 반영)

아래는 TK가 이미 확인한 확정 수정 과제다. 발견 여부와 무관하게 반드시 처리하고, 수정 후 Phase 6 기준으로 재검증한다.

1. **캐릭터 3D 통일:** 현재 **첫 화면은 2D 캐릭터, 이후 화면부터는 3D 캐릭터**로 불일치한다. 앱 전체를 **3D 캐릭터로 일관되게 통일**한다.
   - 첫 화면(스플래시/온보딩/인트로 등)의 2D 캐릭터 애셋 사용처를 찾아, 다른 화면에서 쓰는 3D 캐릭터와 **동일한 스타일·톤·렌더링**으로 교체한다.
   - 첫 화면뿐 아니라 **전 화면을 훑어 2D/3D 혼재를 모두 색출**해 3D로 통일한다(로딩·빈 상태·모달·알림 일러스트 포함).
   - 3D 애셋이 없으면 임의 생성하지 말고, 기존 3D 캐릭터 애셋을 재사용하거나 부족한 애셋을 목록으로 보고한다.

2. **문구 수정 — 어색한 표현 정리:** **"한 가족 두 시점"** 같은 어색하거나 뜻이 모호한 표현을 자연스럽게 다듬는다.
   - 앱 전체 문구에서 해당 표현과 **유사한 어색한 마케팅/UX 카피를 모두 색출**한다(온보딩·소개·설정 설명·에러 문구 포함).
   - 각 항목마다 **자연스러운 한국어 대안 2~3개를 제안**한다. 명백한 오탈자·문법 오류는 바로 수정하고, **브랜드 보이스가 걸린 판단성 문구는 적용하지 말고 TK 선택용으로 목록화**한다(임의로 브랜드 카피를 확정하지 않는다).

---

## Phase 0 — 셋업 & 인벤토리
- `adb devices`로 A17·razr 연결 확인, 각 기기에 최신 빌드 실행/설치 확인.
- 안드로이드 UI 자동화 도구와 웹 Playwright 하네스를 세운다.
- Cloudflare Workers/D1/Durable Objects/R2 로그·조회 접근을 확인(백엔드 단언용).
- 레포 구조·라우팅·엔티티(가족/부모/아이/구독)·권한 모델·실시간 채널 구조를 파악해 요약.
- 각 기기에서 앱 콜드 스타트 스모크 1회.
**게이트:** 두 안드로이드 기기 + 웹이 모두 구동되고, 자동화 하네스와 백엔드 조회가 동작.

## Phase 1 — 전 기능 인벤토리 & 스모크 (트리아지)
- 모든 화면·기능을 열거하고 각각 빠른 스모크(진입·크래시·막다른 길 여부).
- **결과로 P0 블로커 우선 목록을 산출**한다. 이후 Phase는 이 우선순위를 따른다.
**게이트:** 전체 기능 지도 + 심각도 태그가 붙은 초기 이슈 목록 확보.

## Phase 2 — 가입 · 로그인 · 소셜연동 · 페어링 · 세션 지속

- **가입 시 아이 정보 수집(충분성):** **이름·생일 등 아이 정보**가 가입 흐름에서 충분히 수집·검증·저장되는가. 필수 필드 누락·형식 오류 처리. 수집한 정보가 부모 화면과 아이모드에 정확히 반영되는가.
- **로그인:** 세션 지속, 토큰 갱신/만료, 로그아웃, 재로그인.
- **소셜연동:** 각 제공자 OAuth(동의 화면까지 UI 자동화로 통과, 테스트 계정), 계정 연결/해제, 중도 취소 처리.
- **페어링(A17↔razr 실기기):** QR 페어링 성공, 재페어링, 해제, 잘못된/만료 코드, **★다른 가족 간 격리(P0 보안)** — A가족 부모가 B가족 아이에 절대 접근 불가.
- **★부모 설정 → 아이모드 반영:** 부모(A17)가 메뉴에서 아이 관련 설정을 변경 → **아이 기기(razr)에 정확히 반영**되는지 2기기 동기로 확인(즉시 또는 재기동 시). 어떤 설정이 언제 반영되는지 매핑.
- **★세션 지속 & 페어링 기억(P1, 실사용 필수):** **로그아웃 후 같은 계정으로 재로그인하면 이전에 연결했던 아이를 자동 인식**(재페어링 불필요)하고, **문제없이 이어서 사용**할 수 있어야 한다. 부모·아이 양쪽 모두. 토큰·로컬 상태가 꼬여 재로그인 후 빈 상태/오류가 나지 않는지 반복 검증.
- **★로그아웃·삭제 시 안내(P1):** 부모 또는 아이가 로그아웃되거나 계정·페어링이 삭제된 경우, **상대측에 적절한 알림/상태 표시**(예: 연결 해제 안내)가 뜨고, 그 시점부터 위치·일정 송수신이 중단되는지 확인.
**통과 기준:** 아이 정보가 충분히 수집·반영되고, 부모 설정이 아이모드에 반영되며, 로그아웃→재로그인 후 이전 아이 인식·정상 사용, 로그아웃/삭제 시 상대측 안내·데이터 중단, 페어링이 가족을 정확히 격리.

## Phase 3 — 구독 · 구독별 서비스 컷 (Qonversion)
- 샌드박스에서 각 티어 구매, 구매 복원, 업/다운그레이드, 만료 처리.
- **엔티틀먼트 매트릭스:** 티어별 어떤 기능이 on/off인지 표로 정리하고 실기기에서 확인.
- **★서버 강제 검증(P0):** 게이팅된 기능을 엔티틀먼트 없이 백엔드로 직접 호출 → **서버가 차단해야** 통과. 클라이언트 UI 숨김만으로는 실패로 판정(매출·보안 직결).
**통과 기준:** 모든 티어의 서비스 컷이 기기·서버 양쪽에서 강제되고, 구매/복원 정상.

## Phase 4 — 부모↔아이 실시간성·정확성 & 위치 · 일정 · 장소 · 하루 경로 (핵심)
2기기 동기 + 백엔드 단언으로 검증한다.
- **실시간성:** 아이 기기 액션 → 부모 기기 반영까지 **지연 예산 내**인가. Durable Object/D1에서도 상태 일치 확인.
- **★페어링 한정 송수신(P0):** 위치·일정 정보가 **페어링된 부모-아이 사이에서만** 송수신되는지. 페어링되지 않은/해제된/다른 가족 간에는 절대 흐르지 않음을 백엔드에서 단언.
- **★위치 정확도 & 부모 확인 UX:** ADB 목 위치 주입으로 이동 재현 → **아이의 정확한 위치**가 확보되는가(오차·갱신 주기·백그라운드·권한 처리). 부모가 그 위치를 **정확하고 간편하게** 파악할 수 있는가(지도 표시 명확성, 최신 위치/시각 표기, 헤매지 않는 진입 동선).
- **★하루 경로(스테이포인트):** 하루 아이 경로가 **쓸데없는 이동 노이즈를 제거**하고 **머무른 장소와 체류 시간**을 지도+목록으로 보여주는가. 검증: GPS 노이즈 필터링·스테이포인트 검출 정확도, 지도와 목록의 데이터 일치, 체류시간 계산 정확도, 경로가 과도하게 잘리거나 중요한 체류지가 누락되지 않는지.
- **★지도 편의성(UX):** 지도 팬/줌, **아이 이름 클릭 시 해당 위치로 지도 이동**, 마커 탭 상호작용, 목록↔지도 연동 등 편의 기능이 자연스럽게 동작하는가. 조작이 버벅이거나 엉뚱한 위치로 튀지 않는지.
- **일정:** 부모 생성 → 아이 반영, 수정/삭제 동기화, 타임존, FCM 리마인더(페어링 한정 송수신 포함).
- **장소(지오펜스):** 진입/이탈 트리거 및 알림 정확도.
- **정확성·격리:** 낡은/틀린 데이터 없음, 가족 스코프 정확.
- **회복력:** 네트워크 끊김→재연결 시 동기화 복구.
**통과 기준:** 실시간 지연 예산 충족·아이 위치 정확·부모 확인 간편, 하루 경로가 노이즈 없이 체류지+시간을 지도·목록으로 정확 표시, 지도 편의 기능 정상, 위치·일정이 페어링 쌍에만 흐름, 네트워크 복구 정상.

## Phase 5 — SOS · 주변소리 듣기 · 친구놀이 · 포스링
- **SOS(P0):** 아이 기기(razr)에서 SOS를 자동 트리거 → 부모 기기(A17)가 알림+위치를 신속 수신. **잠금화면·열악한 네트워크**를 ADB로 조성해 그 조건에서도 동작하는지 + 백엔드 전달 경로를 단언.
- **★주변소리 듣기(원격 오디오 모니터링, P0):** 부모가 아이 주변 소리를 듣는 기능이 **어떤 상황에서도** 안정적으로 동작해야 한다. ADB로 다양한 조건을 조성해 검증: **잠금화면, 백그라운드, 앱 종료 상태, 열악/불안정 네트워크, 재연결, 권한 상태, 장시간 세션.** 연결 실패·무음·끊김·지연이 없는지, 실패 시 재시도/복구가 되는지 확인. 프라이버시·동의 처리도 함께 점검.
- **포스링:** 원격 강제 벨소리 동작.
- **친구놀이:** 요청/수락 플로우가 계정 간에 완결되는가.
**통과 기준:** SOS·주변소리 듣기가 잠금·백그라운드·종료·열악 네트워크 등 악조건에서도 신뢰성 있게 동작, 포스링·놀이 코디 플로우 완결.

## Phase 6 — 디자인 · 문구 · 설정 · 사용성 · UI/UX
- **디자인 시스템 충실도:** Wanted DS·Pretendard 토큰이 각 기기·해상도에서 지켜지는가. **★razr 폴더블 화면비**와 **iOS Safari**에서 레이아웃 파손·잘림·겹침 없는지 집중.
- **★캐릭터 3D 일관성:** 사전 지정 필수 수정 항목 1을 반영한 뒤, 첫 화면 포함 전 화면에서 캐릭터가 3D로 통일되었는지 스크린샷으로 확인(2D 잔존 0).
- **★문구 정합성:** 사전 지정 필수 수정 항목 2를 반영한 뒤, "한 가족 두 시점" 등 어색한 표현이 정리됐는지 확인.
- **메뉴·설명 문구:** 라벨·설명·에러 메시지의 정확성·일관성·한국어 톤, 오탈자, 플레이스홀더 잔존, 오버플로우/잘림.
- **설정:** 모든 토글이 실제로 동작하고 값이 지속되는가.
- **사용성/접근성:** 탭 타깃 크기, 대비, 내비게이션 명료성, 빈/에러/로딩 상태.
**통과 기준:** 폴더블·iOS 포함 어떤 기기에서도 시각 파손 없음, 문구 클린, 설정 지속.

## Phase 7 — 최적화 · 성능 · 안정성
- 콜드/웜 스타트 시간, 화면 전환 버벅임, 메모리, 백그라운드 위치의 배터리 영향, 네트워크 페이로드 크기.
- 장시간 소크, 백그라운드 전환, 저전력, 오프라인→온라인 전환에서 **크래시 프리**.
**통과 기준:** 성능 예산 내, 소크 구간 크래시 0.

## Phase 8 — 적대적 · 엣지 (릴리즈 하드닝)
- **네트워크:** 오프라인·불안정·기내모드 중간 전환, 토큰 만료, 요청 중 강제 종료.
- **권한:** 사용 중 권한 거부/회수, 클럭 스큐, 중복 탭/레이스, 딥링크·알림 탭 콜드 스타트.
- **관계 엣지:** 부모 2명 1아이, 구독 없는 아이, 스토어 계정 엣지.
- **★보안·프라이버시(P0):** 가족 간 데이터 격리, 실시간 채널 인증, 클라이언트에 시크릿 노출 여부, 미성년자 PII 처리.
**통과 기준:** 우아한 처리, 데이터 유출·크래시·멈춤 없음.

## Phase 9 — 수정 루프 & 회귀
- P0 → P1 순으로 수정. 각 수정 후 **해당 플로우를 실기기에서 재검증**하고 인접 기능 회귀 확인.
- 파괴적/프로덕션 반영·스토어 배포는 하지 않는다(제안·준비까지만, 실행은 TK 승인).

## 종료: 릴리즈 게이트 리포트 (Go/No-Go)
1. Phase별 pass/fail + 근거(스크린샷·로그).
2. 이슈 대장: 심각도·상태·수정 diff·재검증 결과.
3. **Go/No-Go 판정**: 위 "완벽의 정의" 기준 충족 여부를 항목별로 명시.
4. 출시 후 fast-follow(P2/P3) 목록.

---

## 실행 가드레일 (내일 출시 — 시간·비용)
- **P0 우선:** 전체를 깊게 파기 전에 Phase 1 스모크로 블로커부터 잡고 고친다. P3에 매몰되지 않는다.
- **Phase 게이트마다 중간 보고 후 진행**(한 번에 끝까지 자율 실행 금지).
- **모든 조작은 ADB로 실제 실행**하고 근거(스크린샷·로그)를 남긴다. 실행하지 않은 결과를 지어내지 않는다.
- **재시도 상한 3회**, 무한 루프 방지.
- **파괴적 작업 금지:** 프로덕션 D1/실사용자 데이터 삭제·스토어 배포·시크릿 변경 금지(샌드박스·테스트 가족만).
- "완벽" = 위 Go/No-Go 게이트. 게이트 충족 시 즉시 종료 판정하고 리포트로 마무리.
