# 2026-09-20 작업 이력

아래 기록의 기존 작업 폴더는 당시 로컬 폴더다. 현재 상태와 다른 컴퓨터 재빌드는 [인계 안내](2026-09-20-rebuild-handoff.md)를 따른다.

**해외 Google 지도 점검·수정(2026-09-20, Pages 배포 완료)**:
현재 이 작업 폴더는 `1e59801`/앱 1.4.2이며, 원격 main은 `2848bf99`/앱 1.4.9다.
구형 폴더에서 다시 웹을 배포하지 않는다. 최신 소스 기반 배포 정본은
`.worktrees/google-maps-audit-20260920` (`fix/google-maps-audit-20260920`)에 있다.
사용자 진행 지시에 따라 최신 Google 지도 구현과 웹 재시도·핀/확대 유지·Google 글꼴 CSP 수정,
이미 운영 중인 설정 로딩 개선·유리 스타일을 합쳐 Pages에 배포했다.
최종 주소는 `https://1cf06a72.hyeni-calendar.pages.dev`, entry는 `assets/index-CqtF0rCH.js`다.
배포별·고정 Pages·브랜드 apex·www에서 14개 자산씩 로컬 해시 일치, OAuth callback·보안 헤더 확인.
12개국 고정 좌표 실제 Google SDK 로딩·집중 회귀 90/90·설정 브라우저 12개 시나리오·build 통과.
Android 지도 수명/배경 수정은 소스만 준비됐으며 앱 설치/스토어 배포는 하지 않았다.
현지 통신망·인증된 해외 장소 저장 E2E는 미검증이고 인앱 Google 도보 경로는 비활성 상태다.
미완료 로그인·세션 변경, 운영 계정·시크릿·Worker·D1·실기기는 변경하지 않았다.
상세 결과는 [지도 점검 보고서](.worktrees/google-maps-audit-20260920/docs/reports/2026-09-20-google-maps-audit.md),
배포 증거는 `artifacts/google-maps-audit-20260920/deployment.json`이다.


**부모 설정 진입 지연·로딩 화면 정리(2026-09-20, Pages 배포 완료)**:
기존 설정은 계정·구독 조회를 모두 기다리며 전체 화면을 `ScreenQueryState`로 교체해 헤더 정렬·배경이 바뀌었다.
이제 설정 헤더·메뉴를 즉시 표시하고 프로필 자리에만 skeleton과 공용 LoaderMark를 둔다. 계정 오류·빈 응답은
`ScreenQueryState`의 선택적 embedded 모드로 프로필 영역에서 재시도하며, 구독 조회 실패는 해당 행 아래에서만
알린다. 미확정 구독을 무료로 표시하지 않고 캐시가 있는 프로필은 백그라운드 실패로 숨기지 않는다.
`useAccount`는 별도 account 캐시 계약을 유지하되 가족 정본 `qk.family`의 `fetchQuery(getMyFamily)`를 공유하고,
재시도도 가족 query 한 곳에서 처리한다. `toAccountInfo`가 동일 응답을 계정 뷰로 변환한다.
앱 typecheck·production build·관련 회귀 41/41 통과. `scripts/verify-settings-loading.mjs`가 격리 fixture로
WebKit·Chromium × 프로필 지연/구독 지연/프로필 오류/구독 오류/빈 응답/캐시 재진입 12개 시나리오와
각 320·390px 화면을 검증했다. 직접 설정 진입의 가족 요청 1회, 신선한 홈 캐시→설정 전환의 추가 요청 0회,
오류 뒤 재시도 복구·가로 overflow 0·pageerror 0 확인. 캡처·결과는 `artifacts/settings-20260920/`다.
사용자 배포 지시에 따라 HEAD `1e59801` 스냅샷에 설정 관련 6개 파일과 이미 배포된 `glass.css`를 반영한
격리 빌드로 Pages `https://182f4455.hyeni-calendar.pages.dev`에 배포했다. 진행 중인 세션 정책 변경은 포함하지 않았다.
배포 대상 typecheck·build·회귀 41/41·브라우저 12개 시나리오를 다시 통과했다. 배포별 주소·고정 Pages·브랜드
도메인의 entry `assets/index-C0JssPFf.js`, CSS `assets/index-Bv8h1S_X.css`, SW와 설정 관련 5개 청크가
로컬과 해시 일치하고 루트·OAuth callback이 200인 것을 총 30개 요청으로 확인했다. 브랜드 도메인을 WebKit으로
열어 격리 fixture 지연 중 설정 메뉴 6행·새 로딩 카드, 응답 뒤 프로필 전환과 overflow/pageerror 0도 확인했다.
증거는 `artifacts/settings-20260920/{release-scope,release-browser-report,deployment,production-browser}.json`이다.
실제 iPhone·실사용 API 지연 검증은 남아 있으며 운영 계정·세션·Worker·D1은 사용하거나 변경하지 않았다.


**부모 모드 투명도 추가 조정(2026-09-20, Pages 배포 완료)**:
TK의 추가 요청으로 유리 채움을 0.24~0.58→0.10~0.34, 맑은 면 0.24→0.12,
버튼 0.48→0.30, 블러 18px→12px로 낮췄다. 글자 잉크·하단 메뉴 채움·접근성 폴백은 유지했다.
진행 중인 별도 로그인/세션 수정이 섞이지 않도록 HEAD 스냅샷에 현재 `glass.css`만 반영해 빌드·배포했다.
타입 검사·production build, 관련 테스트 22/22, WebKit·Chromium 홈/설정 및 320px 홈 검증을 통과했다.
Chromium 투명도 감소 시 흰 배경·블러 없음도 확인했다. 배포는
`https://5d4a1d9b.hyeni-calendar.pages.dev`이며 배포별·고정 Pages·브랜드 도메인의 JS/CSS/SW 해시가
격리 빌드와 일치한다. 캡처와 배포 증거는 `artifacts/glass-clear-20260920/`에 있다.
Worker·D1·실사용 세션은 변경하지 않았다.

**부모 로그인 시간 만료 점검·수정(2026-09-20, 로컬 검증·미배포)**:
기존 access JWT는 1시간마다 자동 갱신하지만 refresh와 활성 설치는 갱신 후 30일에 만료돼,
부모가 오래 미사용하면 다른 기기 로그인 없이도 로그아웃될 수 있었다. `sessionLifetime.ts`의 지속 만료
표식으로 새 부모 로그인과 같은 기기 회전을 유지한다. 직접 로그아웃·새 기기 로그인·권한 철회는 유지하고
아이·익명·선생님 수명은 바꾸지 않았다. 클라이언트도 refresh의 알 수 없는 401/403·HTML 응답·기기 준비 오류를
세션 철회로 오판하지 않고, 명시적 `invalid_token`/`device_session_inactive`만 세션 종료로 분류한다.
기존 부모 전환은 `worker/db/persistent-parent-sessions.sql`이며 활성 설치의 미만료·미철회 토큰만 연장한다.
이미 만료된 세션은 부활시키지 않는다. 스키마 변경이 아닌 데이터 정책 전환이므로 표식을 보존하는 Worker 배포
뒤 SQL을 적용하고 웹을 배포한다. 실사용 세션 보호 규칙에 따라 운영 SQL·토큰 접근·배포·실기기 로그인은 수행하지 않았다.
서버 집중 회귀 20/20, 앱 집중 회귀 18/18, 앱·Worker typecheck와 build 통과. 전체 앱 1,998/1,999의
1건은 Java Runtime 부재로 Android manifest 생성 불가다. 전체 Worker 1,298/1,307의 9건은 결제·위치 보관
테스트이며 격리된 수정 전 HEAD에서도 같은 9건 실패를 재현했다(마지막 추가 회귀 1건은 집중 실행에 포함).


**부모 모드 맑은 유리 재질 보강(2026-09-20, Pages 배포 완료)**:
아이폰에서 기존 화면이 불투명한 서리판처럼 보인다는 TK 요청에 따라 공용 `src/styles/glass.css`의
흰 채움 0.62를 반사 방향에 따른 0.24~0.58로 바꾸고, 블러 28px→18px·채도 180%→150%로 조정했다.
밝은 모서리와 부드러운 그림자로 두께를 표현하며, 배경의 하늘색·민트와 하단 색을 조금 더 살린다.
이번 요청의 맑은 유리 표현이 과거의 단색 표면 제한보다 우선한다. 화면별 CSS에 효과를 복제하지 않는다.
본문 잉크·신호색·데이터·아이 모드는 유지한다. 하단 탭은 WebKit 합성에서 뒤 글자가 비치는 상황도 고려해
별도 0.9 채움으로 가독성을 보호한다. 투명도 감소·backdrop 미지원은 공용 토큰 자체를 불투명으로 바꿔
나중에 로드되는 부모 홈·상세 화면까지 같은 폴백을 받는다.
디자인·표면·대비 관련 기존 회귀 69/69와 앱 typecheck·production build가 통과했다.
격리 fixture로 WebKit·Chromium 부모 홈·달력·설정·알림과 320px 홈에서 가로 넘침·페이지 오류가 없음을 확인했다.
Chromium의 투명도 감소 설정에서는 흰 배경·블러 없음으로 전환됨을 확인했다. 캡처·결과는
`artifacts/glass-20260920/`에 있다. 사용자 배포 지시 후 Pages
`https://ee3aaf81.hyeni-calendar.pages.dev`에 배포했다. 배포별 주소·고정 Pages·브랜드 도메인 모두
entry `assets/index-DJ9o8E42.js`, CSS `assets/index-7nt2EIMU.css`, `sw.js`가 로컬 dist와 SHA-256 일치하고
루트·OAuth callback 200 및 같은 entry 참조를 확인했다. 브랜드 HTML에는 Cloudflare 분석 스크립트가
추가되어 HTML 전체 해시는 다르다. 검증 결과는 `artifacts/glass-20260920/deployment.json`이다.
실제 아이폰 검증은 남아 있으며 운영 계정·세션·Worker·D1은 변경하지 않았다.
